import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Bubble Pop app exists', (WidgetTester tester) async {
    // Placeholder — full integration tests require GetStorage init
    expect(true, isTrue);
  });
}
